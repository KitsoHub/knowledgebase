# Knowledge base

### TODO:

- fix api for submission no data is submitted
-        "dev": "next dev --turbopack && GENERATE_SOURCEMAP=false",

### Installs
- For managing sites configurations and tests
- mock service worker
- whatwg encoding standard
- jest
setting up jest requires config
- json
- jest.config.ts
npm i -D jest @types/jest ts-jest msw whatwg-fetch @testing-library/react @testing-library/jest-dom swr
ts-node
jsdom
- embla-carousel-react
